#version 330 core

// Input vertex data, only point reticule
layout(location = 0) in vec3 vertexPosition_modelspace;

// Output to fragment shader
out vec4 v_color;

void main()
{
	gl_Position =  vec4(vertexPosition_modelspace,1);
}

